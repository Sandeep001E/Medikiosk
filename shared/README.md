# Shared Contracts

Place cross-dashboard constants, validation schemas, and shared data contracts here as the project evolves. The current application preserves existing dashboard-specific source files while the unified backend provides the shared API and database boundary.
